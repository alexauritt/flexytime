<%= form_for(@greet) do |f| %>
  <% if @greet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@greet.errors.count, "error") %> prohibited this greet from being saved:</h2>

      <ul>
      <% @greet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
